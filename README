MLDSP aims to become a visual music player. So far, it only visualizes SMF.

It depends on tsukimi. The primary UI was initially created using tsukimi,
but it's not automatically generated anymore (I've added changes manually).

It's not working beautifully yet.
